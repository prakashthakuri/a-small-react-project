const initialState = {

    canvasStar: [

    ]
}

export default function opacityReducer ( state = initialState, action) {

        switch(action.type) 
        {
            case 'STORE_STARS': 
                return {
                    ...
                }
        }
}   